extends layout

block content
    div.text-center
        form(class="form-signin" method="POST" action="/login")
            #error
                if error
                    p.text-danger Error!!!
            - const h1Classes = ['h3', 'mb-3', 'font-weight-normal']
              h1(class=h1Classes) Please login
            //-input email
            label( for="inputEmail" class="sr-only") User name
            //-input(type="email" name="username" id="UName" class="form-control" placeholder="User Name" required autofocus)
            input(type="text" name="username" id="UName" class="form-control" placeholder="User Name" required autofocus)
            //-input password
            label(for="inputPassword" class="sr-only") Password
            input(type="password" name="password" id="UPass" class="form-control" placeholder="Password" required)
            //-remember Checkbox
            //- const divClasses=['checkbox', 'mb-3'];
            //-div(class=divClasses)
            //-    label
            //-        input(type="checkbox" value="remember-me")
            //-        span Remember me
            //-signIn button
            - const buttonClass=['btn', 'btn-lg', 'btn-primary', 'btn-block'];
            button(class=buttonClass type="submit") Login
            - const pClasses=['mt-5', 'mb-3', 'text-muted'];
            
            p(class=pClasses) © SFSU-CSC867 - Demonstration ONLY  2021